// Open Vector — Content Aggregator
// Learn content now comes from virtual:learn-content (Vite plugin).
// This file provides non-lesson content for the landing page and other static pages.

import open from './open';
import recommendedReading from './recommended-reading';

const en = { open, recommendedReading };

export default en;
